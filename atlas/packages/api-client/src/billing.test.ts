import {it,expect} from 'vitest';
import {createApiClient,type MockRuntimeConfig} from './index.js';
it('Free bloqueia a terceira ficha e Pro libera sem recriar o adapter',async()=>{const runtime:MockRuntimeConfig={seed:42,latencyMs:0,errorRate:0,plan:'free',role:'athlete'};const api=createApiClient({mode:'mock',mockRuntime:runtime});expect((await api.programming.listPlans()).items).toHaveLength(2);await expect(api.programming.createPlan({name:'Terceira ficha',goal:'strength'})).rejects.toMatchObject({code:'quotaExceeded'});expect(await api.identity.hasEntitlement('interactiveAnatomy')).toBe(false);runtime.plan='pro';runtime.role='professional';expect((await api.identity.getMe()).planKey).toBe('pro');expect((await api.identity.getMe()).roles).toContain('professional');expect(await api.identity.hasEntitlement('interactiveAnatomy')).toBe(true);expect((await api.programming.createPlan({name:'Terceira ficha',goal:'strength'})).status).toBe('draft');runtime.plan='free';expect(await api.identity.hasEntitlement('interactiveAnatomy')).toBe(false);});
